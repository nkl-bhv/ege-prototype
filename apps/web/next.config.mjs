/** @type {import('next').NextConfig} */
const nextConfig = {
  // Ничего лишнего. Не используем experimental.serverActions —
  // в Next 14 это включено по умолчанию.
  // Если нужно, можно добавить свои опции сюда.
};

export default nextConfig;

